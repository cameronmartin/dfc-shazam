name: "python_ml_scipy"
description: "ML app with NumPy and SciPy"
language: "python"
complexity: "complex"
tags: [python, ml, numpy, scipy, apt]

source:
  base_image: "python:3.11-slim"
  package_manager: "apt"
  packages: [build-essential, gfortran, libopenblas-dev]

expected:
  base_image: "cgr.dev/chainguard/python:latest-dev"
  packages: [build-base, gfortran, openblas-dev]

build:
  dockerfile: "Dockerfile"

runtime:
  enabled: false
