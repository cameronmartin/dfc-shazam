name: "centos_yum"
description: "CentOS-based image using yum"
language: "python"
complexity: "simple"
tags: [centos, yum, python]

source:
  base_image: "centos:7"
  package_manager: "yum"
  packages: [python3, python3-pip, gcc, openssl-devel]

expected:
  base_image: "cgr.dev/chainguard/python:latest-dev"
  packages: [python-3, py3-pip, gcc, openssl-dev]

build:
  dockerfile: "Dockerfile"

runtime:
  enabled: false
